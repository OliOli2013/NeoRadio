# NeoRadio v2.1 - hotfix

Poprawka startu wtyczki po błędzie:

- naprawiono błąd `name 'to_text' is not defined`, który blokował ładowanie NeoRadio w menu Wtyczki,
- funkcja `to_text()` została przeniesiona przed inicjalizację konfiguracji `config.plugins.neoradio`,
- wersja pozostaje 2.1,
- baza stacji, Radio-Browser Online, czyszczenie `/tmp` i poprawki głośności pozostają bez zmian.
